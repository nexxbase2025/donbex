Base exacta del usuario:
radioapp-main (2)JUEVES AGOSTO27.zip

Cambios limitados:
1. QR:
   - descarga real como PNG attachment;
   - nombre basado en slug;
   - se fuerza cache-bust del JS;
   - iOS guarda web-downloads en Archivos > Descargas.
     Safari NO permite que una web escriba silenciosamente en Fotos.
2. Ruta /edit antigua:
   - ya no se usa;
   - cualquier link /edit/<token> redirige automáticamente al panel /order/<requestId>;
   - se oculta la flecha antigua "Volver a mi panel".
3. Pantalla tras pago:
   - queda escuchando solo su solicitud;
   - cuando Admin aprueba, cambia automáticamente usando el render existente de solicitud aprobada.

No se modificaron:
- templates;
- player;
- PWA;
- guardado inline;
- Firebase rules;
- checkout visual;
- Admin.
