DONBEX RADIO APP V17 — TRACKING + TRUE MOBILE CHECKOUT

Key changes:
- On touch phones, checkout opens as a TRUE full-screen page, not a centered modal.
- Dedicated tracking page: no Studio controls are shown on /order/<token>.
- Tracking page reloads the saved/live station config: template, name, logo, stream and socials.
- Approved tracking shows public URL and QR action.
- Tracking includes edition/renewal plan choices compatible with DONBEX Admin V3.0.
- Public radio continues separately at /<slug>.
- No changes to Firebase project IDs, payment base flow, approved radio documents, or the 4 player templates.

Note:
The renewal buttons are UI-prepared here. The final payment upload form for renewals will reuse the existing checkout/payment fields in the next coordinated step once this tracking/full-screen mobile behavior is confirmed.
